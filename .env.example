# FlowFund - Local Docker Environment Variables

# --- Database (Local Postgres) ---
# Default values used in docker-compose.yml
POSTGRES_DB=flowfund
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# --- Google OAuth (Get these from Google Cloud Console) ---
# See walkthrough.md for instructions on setting up Google OAuth
GOOGLE_CLIENT_ID=329145643510-o0fqkat62pfokdhk62k0ifloc3u5l9qn.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# --- App Configuration ---
# Internal URLs (Used by Docker services to talk to each other)
# These are already handled in docker-compose.yml via environment mapping
# but you can override them here if needed.
# SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/flowfund
# ARIMA_API_URL=http://ai-service:8000
# FRONTEND_URL=http://localhost:3000
# VITE_API_BASE_URL=http://localhost:8080/api
